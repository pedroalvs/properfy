services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    ports:
      - "4000:3000"
    env_file:
      - apps/backend/.env
    environment:
      - NODE_ENV=development
      - PORT=3000

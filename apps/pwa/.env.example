# API base URL
VITE_API_BASE_URL=http://localhost:3000

# Mapbox token for map views (property map, service group map).
# Obtain from: https://account.mapbox.com → Access tokens → Create a new token (public scope).
# Without this token map views will display an error instead of rendering.
VITE_MAPBOX_TOKEN=
